{
  "builds": [
    {
      "src": "champdle/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/", "dest": "/lumidle/index.html" },
    { "src": "/lumidle/(.*)", "dest": "/$1" }
  ]
}