{
  "installCommand": "pnpm install && cd lumidle && pnpm install",
  "buildCommand": "cd lumidle && pnpm run build",
  "outputDirectory": "lumidle/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
